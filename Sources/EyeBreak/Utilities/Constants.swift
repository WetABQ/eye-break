import Foundation

enum Constants {
    static let defaultWorkDuration: TimeInterval = 20 * 60  // 20 minutes
    static let defaultBreakDuration: TimeInterval = 20       // 20 seconds
    static let defaultIdleThreshold: TimeInterval = 30       // 30 seconds
    static let timerTickInterval: TimeInterval = 1.0
    static let permissionPollInterval: TimeInterval = 2.0
}
